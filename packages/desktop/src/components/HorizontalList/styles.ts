import styled from 'styled-components'

export const Container = styled.div`
  position: absolute;
  margin-top: 0;
  margin-left: 0;
  /* left: 0; */

  .slider__items {
    display: flex;
    will-change: transform;
  }
`
